# Установите в виртуальное окружение: pip install -r requirements.txt
# Версии зафиксированы ориентировочно; при конфликтах обновите под свою систему.

numpy>=1.24
attrs>=23.0
pandas>=2.0
polars>=0.20
scikit-learn>=1.3
matplotlib>=3.7
jupyter>=1.0
ipykernel>=6.0

# Лабы 3–4, 7 (PyTorch)
torch>=2.0
torchvision>=0.15

# Лаба 7 (Hugging Face)
datasets>=2.14
huggingface-hub>=0.19

# Лаба 7 (по коду ноутбука — при необходимости)
librosa>=0.10
catboost>=1.2
